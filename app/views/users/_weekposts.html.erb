<h2>7日間分の投稿数</h2>
<table class='table table-bordered'>
  <thead>
      <tr>
        <% past_week_books.each do |data| %>
        <th>
          <% if (Date.current - data[:date]).to_i.zero? %>
            今日
          <% else %>
            <%= "#{(Date.current - data[:date]).to_i}日前" %>
          <% end %>
        <% end %>
        </th>
    </thead>
    <tbody>
      <tr>
        <% past_week_books.each do |data| %>
        <td><%= data[:count] %></td>
        <% end %>
      </tr>
    </tbody>
</table>